# DiceFunc 仓库约定

## 产品范围

DiceFunc 是面向 QQ 官方机器人的 Cloudflare Workers 跑团服务，使用 TypeScript、ESM 和 pnpm workspace。当前运行平台只包括 Cloudflare Workers，HTTP 接入使用 Hono；单个部署实例默认绑定一个 QQ App，通过配置和内容包提供不同风格。

除非任务明确扩展范围，不新增 WebUI、管理 REST API、产品 CLI、其他社交平台、扫码登录、托管多租户、常驻系统服务、容器部署、自动更新、运维控制台、跨云灾备或不可信插件沙箱。配置编译和发布能力作为内部模块或构建流程提供，不建设面向用户的命令行产品。不要为暂缓平台预建通用云框架，也不要把完整 SealDice 兼容当作默认承诺。

`SEALDICE_FEATURES.md` 只用于确定迁移范围，不能作为命令语义的完整规格。`sealdice-core/` 若存在，只作为只读兼容参考，不是运行时依赖；原设计采用提交 `4493693` 作为兼容基线，本地检出可能已经漂移，开展兼容工作前必须核对目标提交。COC7 等行为应对照对应源码、现有测试和新增兼容用例，不能只依据功能清单摘要。

实现状态以当前源码、迁移和可执行测试为准。README 和 docs 中的规划、清单或示例不构成已经实现或已经在线验证的证据；修改行为时同步修正相关文档，明确区分已实现、规划中、本地验证和云端验证。

## 目录与依赖边界

- `apps/worker` 是 Worker 组合根，包含 `fetch`、`queue` 和 `scheduled` 入口、Hono 路由及 Cloudflare bindings。
- `packages/core` 包含领域对象、应用服务和 ports。领域层不得导入 Hono、QQ SDK、Cloudflare 类型、适配器或配置解析器。
- `packages/adapters` 实现 QQ、D1、R2、Queues 和运行时日志等 ports，不得把供应商类型反向泄漏进 core。
- `packages/config` 负责 YAML 校验、规范化配置包和版本发布，不承载游戏状态。
- `migrations` 是 D1 架构的唯一迁移链；请求处理器不得自动执行迁移。
- `tests/runtime` 验证纯业务与受控替身协作，`tests/integration` 在 workerd 环境验证真实 D1、KV、R2 和 Queues bindings。

依赖方向保持为 `apps -> adapters + config + core`、`adapters -> core ports`。配置编译器可以引用公开命令和模板契约，core 不反向依赖配置编译器。应用入口显式组合依赖；没有实际需求时不引入 DI 框架、事件总线、通用工作流引擎或微服务拆分。

围绕业务概念维护领域边界：会话、角色卡、规则集、战斗、牌堆、跑团日志和权限对象负责自己的状态与不变量；应用服务编排命令和副作用；公式计算、判定、解析和文本转换优先保持为纯函数。业务层返回结构化结果和回复事件，不直接拼装平台文案，也不直接访问 QQ、R2 或任意 SQL。

Workers 代码不能依赖常驻内存、进程锁、本地磁盘、后台无限循环或 Node 原生扩展。固定 compatibility date，新增兼容开关需有明确原因和测试。`waitUntil` 不能替代持久队列。

## QQ 接入契约

- `POST /webhooks/qq` 必须先读取并保留原始请求字节，再按官方算法进行签名处理。区分地址验证 `op=13`、事件 `op=0` 和确认 `op=12`；不得先重序列化 JSON 再验签。
- 地址验证、群聊 @、C2C 和其他场景分别建模。群被动回复的时限、次数及 `msg_id + msg_seq` 约束不能套用到其他场景；Markdown、富媒体和主动消息必须在实际权限验证后才能启用。
- 只记录平台实际送达本服务的事件。没有测试 App 的真实回调证据时，不假设已获得普通群消息全量事件。
- 暗骰不能在私聊失败后回落为群公开结果。未经可信映射的群身份和 C2C 身份不得合并，也不得把 OpenID 当作跨 App、跨群或跨场景的通用 QQ 号码。
- 管理员身份来自带 bot、场景和会话作用域的配置或权威状态，不得从昵称、消息文本或未经验证的平台角色推断。机器人关闭时，授权恢复命令仍须可用且不能绕过权限检查。
- 未识别但合法的事件按协议确认并忽略。拒绝、重试和 ACK 行为必须与 QQ 协议及当前回复窗口一致；超时后不得擅自改用主动消息。

## 状态、事务与异步恢复

D1 是角色卡、会话、权限、事件去重、命令结果、任务、日志索引和配置发布记录的权威存储。KV 只缓存可容忍最终一致的不可变配置或版本指针，不能用于扣 SAN、无放回抽牌、事件去重、锁、权限撤销或其他权威状态。R2 保存私有配置包和跑团日志对象，二者使用独立 bucket。Queues 至少一次投递，Cron 只做有界补投、恢复和清理。

一次业务处理遵守以下顺序：

1. 校验请求、App、场景和稳定事件标识，在 D1 原子保存接收事实、配置摘要、持久随机材料、会话序号和任务意图。
2. 等待队列投递成功后再返回成功 ACK；D1 与 Queues 没有跨服务事务，补投必须能恢复“已写库但未入队”的状态。
3. 消费者用 D1 租约和单调递增的 fencing token 占用任务，按事件固定的配置版本与随机流加载快照。
4. 以有限、类型化的 `CommandCommit` 原子提交业务状态、结果、日志条目、回复意图和后续任务；提交成功后才能执行 QQ 或 R2 副作用。
5. 重复、乱序、租约过期和中途失败通过持久状态恢复。已提交任务只推进未完成副作用，不重新计算业务结果或重新掷骰。

D1 原子更新采用“读快照 -> TypeScript 计算 -> 预构建参数化 SQL batch 校验并提交”。版本集合、聚合范围、会话序号、任务状态和 fencing token 都要进入守卫；只执行 `UPDATE ... WHERE version = ?` 却不验证影响行数不算冲突失败。守卫失败必须让整个 batch 回滚。不能假设交互式事务、行锁、PostgreSQL 存储过程或其他数据库方言在 D1 可用。

所有资源键和唯一约束保留 `bot_id` 作用域。共享卡、范围查询、创建和删除需校验相关聚合版本，避免只保护最后写入的单行。提交超时先按 execution ID 查询结果，不能直接假定回滚。跨群共享角色卡必须显式告知用户并校验同一所有者；默认群内卡隔离。

同会话命令按 D1 接收序号推进，终态失败也必须解除后续阻塞。跨服务效果只能保证可追踪和可恢复，不宣称端到端 exactly-once。回复意图需要冻结文本、模板或配置摘要、目标场景、原消息 ID、稳定分段序号和截止时间；超时或未知结果不得换 `msg_seq` 绕过平台去重。

队列消息只携带内部任务 ID、bot ID、任务类型和必要版本，不放聊天正文、凭据、下载 token 或随机种子。重试、scheduled 补投和死信处理共用 D1 中的尝试预算、截止时间、租约和终态。

## 配置、模板与扩展

配置使用 YAML，经严格 Schema 校验后编译为规范化、不可变的运行时包。拒绝未知字段、重复键、非法权重、循环继承、悬空引用和无限别名展开；错误需定位到文件及字段路径。业务配置只能选择已在 Wrangler 声明的 bindings，不能自行创建云资源。

设置合并顺序为内置默认值、全局文件、群文件、数据库中允许的运行时覆盖。映射按字段覆盖，数组整体替换，`null` 仅在 Schema 明确允许时有意义。权限、安全上限、Secrets、bindings 和配置源不能被群命令覆盖；限制值取系统硬上限与配置值中更严格者。文件是默认值和硬约束来源，D1 是可变游戏状态来源，运行时命令不得回写 YAML。

每个事件固定配置摘要，旧任务必须能按摘要重放。D1 是发布版本和 revision 的权威来源；KV 只是缓存。配置包缺失、摘要不符、Schema 不兼容或引用不完整时整体拒绝，不能部分切换，也不能用任意默认值继续处理命令。Secrets 不进入配置包。

模板只接收经 Schema 验证的结构化字段，不能访问数据库、网络、原型链、动态 include、任意表达式或再次掷骰。模板随机流与业务随机流分离，重试复用已选 variant 和最终文本。人物名称、原因等用户数据只作为值插入，不再次解析为模板。QQ 转义和平台控制字段由适配器处理。

命令处理顺序保持为权限与内容检查、核心命令、当前规则命令、启用扩展、非命令自定义回复。别名冲突必须显式解决，不能依赖文件加载顺序；普通自定义回复不得截获管理命令。匹配、表达式、递归、骰数、输出和 CPU 使用共享有界预算，不使用 `eval`，也不靠 JS timeout 防止同步灾难性回溯。

随机整数使用 Web Crypto 与拒绝采样。持久事件派生相互独立的业务和文案随机流，不自创 PRNG；增加文案候选不能改变业务骰点。规则状态变化集中维护领域不变量，例如 SAN、HP、临时 HP、死亡豁免、法术位、先攻和无放回牌堆，不能散落在命令字符串处理中。

扩展是构建时可信 TypeScript 模块或声明式内容包，与宿主同权限，不得描述为安全沙箱。钩子只能使用受限上下文，不能绕过统一授权、预算和持久化入口。内容包检查依赖循环、路径穿越、数量、大小、版本和素材授权；不下载远程 JavaScript 执行。

## 日志与敏感数据

运行时诊断和跑团正文必须分别建模、存储、授权和清理。Workers Logs 只存结构化诊断；跑团正文存入私有 R2，D1 仅暂存待归档正文、权威索引和访问审计。平台日志保留期不能充当业务审计或跑团日志保留保证。

所有应用诊断通过 `RuntimeLogger`。除该适配器的实际输出实现外，不直接调用 `console`。日志先执行字段白名单，再做大小限制；不得记录 AppSecret、token、Authorization、Cookie、完整 URL 或 query、下载链接、环境对象、原始请求或响应体、昵称、OpenID、人物卡、聊天正文、模板结果及第三方错误正文。参与者关联只能使用专用秘密生成的不可枚举伪名。任务状态和安全审计必须保存在 D1，不能依赖可能采样或丢失的 Workers Logs。

跑团日志默认关闭，只记录服务实际收到的消息和已知发送状态。持续归档遵守以下不变量：

- D1 原子写入去重后的日志项和归档任务，未确认发送的消息保留真实的 pending、unknown 或 failed 状态。
- 消费者先固定连续序号范围，再生成有界、不可变分片；对象键只用内部 ID、序号范围和 SHA-256，不含群名、昵称或 OpenID。
- R2 写入后核对摘要与大小，校验成功并推进连续水位后才能清理 D1 正文。相同范围出现不同摘要属于不变量错误。
- 缺片、摘要错误或正文积压时保持不可下载；超过保护阈值应持久化暂停记录并提示缺口，不能静默丢弃。
- 只有冻结游标范围内记录连续、摘要有效且 manifest 完整时，归档才能进入 `ready`。导出必须说明捕获模式、游标、已知缺口和发送状态。

`STORY_LOG_BUCKET` 与 `CONFIG_BUCKET` 必须隔离并保持私有，不开放公共域名、匿名列表或宽泛 CORS。归档统一经 `GET /archives/:id` 鉴权下载：令牌至少 256 bit，D1 只保存哈希、scope、归档绑定、期限和撤销状态；授权和每次下载尝试先写无正文审计，审计失败时拒绝正文。响应保持私有且不可缓存，不泄露对象是否存在。不要将私有 R2 描述为端到端加密，也不要声称撤销能收回已经下载的副本。

删除流程先在 D1 标记 deleting、撤销授权并停止新写入，再等待旧租约失效并异步删除 R2 对象、D1 正文副本和相关待发内容。上传与清理都检查 generation 或等价版本。失败时维持不可下载的 deleting 状态；确认受限前缀和数据库副本都已收敛后才能标记 deleted。角色卡等独立业务数据不得随剧情日志删除。

Secrets 默认通过本地 1Password 引用注入，云端使用 Workers Secrets 和资源 bindings；Git 只保存 `op://` 引用、非敏感模板和占位值。不得把明文 Secret 放入配置包、仓库、命令参数、测试 fixture、日志或交付内容。

## 测试与完成标准

在仓库根目录使用当前已存在的入口：

```text
pnpm check
pnpm test:runtime
pnpm test:integration
pnpm test:unit
```

`pnpm check` 当前执行 TypeScript project build 和 `biome check .`；不要声称它还运行配置检查或其他不存在的步骤。产品 CLI 不在范围内；新增内部检查入口时，先接入根脚本和文档，再在此更新命令。TypeScript 和 JavaScript 改动按根 `biome.json` 对本次修改范围执行检查与格式化。

业务逻辑修改先用失败测试固定正常、边界、错误和状态变化，再实现。纯计算优先放入 runtime 测试；涉及 D1、KV、R2、Queues、Worker 入口或跨模块副作用的行为必须使用隔离、可重复初始化的 workerd 集成测试，从公共接口验证真实协作和失败路径。第三方 QQ/OpenAPI 使用受控 HTTP 替身，真实平台测试使用独立测试 App 和独立 Cloudflare 资源，禁止连接生产。

按改动风险覆盖以下不变量：

- webhook 原始字节验签、challenge、缺失或无效凭据、重复事件和场景映射；
- D1 batch 整批回滚、版本冲突、同会话乱序、租约过期和旧 fencing token 失效；
- 重试、中断和超时不重复扣减、不重新掷骰、不更换已冻结回复；
- 表达式、模板、牌堆和输出预算对有效、无效、递归及超限输入均有验证；
- 日志测试注入假 Secret、token、身份和正文标记，并断言实际输出中不存在这些值；
- R2 分片重复投递、校验失败、D1 回写失败、缺片、未授权下载、审计失败、删除与上传竞争及重建一致性。

本地 workerd 证据可以证明本地 D1、KV、R2、Queues 和 Web Crypto 协作，不能证明 Cloudflare 多节点限制、线上队列投递与延迟、Workers Logs 实际采样、QQ 权限、公网 webhook、真实回复或生产负载。交付时明确列出已运行命令及结果，并把缺少资源或凭据的外部链路标为未验证。
