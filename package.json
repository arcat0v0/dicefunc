{
  "name": "dicefunc",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "check": "tsc -b && biome check . && pnpm run --filter dice check",
    "test:unit": "vitest run",
    "test:integration": "vitest run -c tests/integration/vitest.config.ts",
    "test:runtime": "vitest run tests/runtime/",
    "dev:worker": "wrangler dev -c apps/worker/wrangler.jsonc",
    "deploy:worker": "wrangler deploy -c apps/worker/wrangler.jsonc",
    "dice": "pnpm run --filter dice cli",
    "cli": "pnpm run --filter dice cli"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@types/node": "^22.0.0",
    "typescript": "^7.0.0",
    "vitest": "^2.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
